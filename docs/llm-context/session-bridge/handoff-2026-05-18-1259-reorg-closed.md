# Handoff — 2026-05-18 12:59

## Goal of the current arc

Multi-phase repo restructure (backlog item 11): **CLOSED.** All five phases (0 research → 4 cleanup) shipped to main. Next arc is the user's pick — newsletter design rework (8j) was the original driver and is the natural next pickup, but the user hasn't committed yet. This handoff carries forward deferred follow-ups + open questions so the next session doesn't relitigate decisions or miss known issues.

## Last actions taken (causal trail, newest first)

- Wrote post-merge doc updates for Phase 4: backlog 11 Phase 4 ✅ (closing the full re-org), AGENT-CONTEXT Phase 4 bullet added to *what works*, pending line for the restructure rewritten as struck-through "✅ Closed 2026-05-18" plus a one-liner noting JSX extraction + full `@website-data` retirement are separate deferred follow-ups (not phases).
- User merged `phase-4-naming-hygiene` → main after Vercel previews went green.
- Built Phase 4 end-to-end on branch. Setup-doc topology notes added (overview + playbook get headers explaining the workspace shape; deeper recipes left alone). 6 superseded docs moved to `docs/client/kol-client-acyr/99-archive/`; empty `docs/client/styleguide/` directory removed. `Acyr.jsx` cleaned: dropped `COLLECTIONS`/`ARTICLES`/`AUTHORS` imports + dependent code, replaced media inventory with "live in Sanity" placeholders. Data files `blog-data.js` + `collections-data.js` deleted. `scripts/migrate-to-sanity.mjs` got a RETIRED header. CSS file renames (`kol-editor.css` → `editor.css`, `kol-typography-fonts-full.css` → `typography-fonts-full.css`) + import updates. Residue sweep: `url(#kol-wheel-*)` → `url(#ac-wheel-*)` in `SpectrumControls.jsx` (caught a latent rendering bug from Phase 1 — clipPath + gradient refs weren't binding); `application/x-kol-library` MIME → `application/x-ac-library`; `--kol-surface-primary` → `--ac-surface-primary`; `kol-combo-fade` → `ac-combo-fade`; 4 dead `docs/kol-migration/` doc-path comments removed.
- Post-merge doc updates for Phase 3: backlog 11 Phase 3 ✅, AGENT-CONTEXT updated with Phase 3 bullet, pending line refreshed to point only at Phase 4, Key Files rows for retired files removed, install-command-override note in contracts updated.
- User merged `phase-3-workspace-cleanup` → main after preview green-light.
- Built Phase 3 end-to-end on branch: deleted `scripts/verify-versions.mjs`, dropped `verify-versions` script from root `package.json`, deleted inert `apps/studio/pnpm-workspace.yaml`, bumped studio React + react-dom 19.2.4 → 19.2.5. `pnpm install` dedupes to single hoisted React 19.2.6 across all six workspace projects. User removed the `cd .. && pnpm install` install-command override on the `kol-client-acyr-styleguide` Vercel project in the UI; preview built green without it.
- Earlier same day: Phase 2 (apps + packages split + pnpm workspace) shipped + editor render fix shipped + post-merge docs. See session log `2026-05-18-phase-2-shipped-and-editor-fix.md`.

## Current state / open decision points

- **Repo restructure CLOSED in backlog + AGENT-CONTEXT.** Phases 0–4 all ✅. No more restructure work tracked as phases.
- **All three Vercel deploys green; all three production sites live.**
- **Two aliases survive in `apps/styleguide/vite.config.js`:**
  - `@components` → `../website/src/components` (JSX extraction deferred per decision Q2)
  - `@website-data` → `../website/src/data` (narrowed scope post-Phase-4 — now only feeds `shop-data.js` for Acyr.jsx product counts; the dead-data refs are gone)
  - Both are documented as "intentional cross-app coupling for now." Neither blocks anything; both die when there's a real motivation to fix.
- **Newsletter pipeline contradiction unresolved.** AGENT-CONTEXT *what works* line 53 says MailerLite domain verification was green at Phase 2 time. The original 8d/8g pending items + user's mid-session comment say verification failed. Whoever picks up 8j next has to actually check MailerLite UI and update the docs.
- **Pre-existing styleguide padding/margin/spacing drift** — confirmed NOT a restructure regression. Deferred until someone wants to A/B against the pre-fold-in reference.
- **`scripts/migrate-to-sanity.mjs`** — broken (data files deleted), kept as reference. RETIRED header in the file says so. Won't run without restoring data files from git history.

## Next intended action

- **First task next session:** ask user what they want to pick up. The natural sequence is newsletter design rework (8j) since it was the original driver for the whole restructure and is now fully unblocked. But the user may want a smaller win first (backlog 1e URL sweep, Metadata Pass 2 for Sanity routes) or something completely different. Don't assume — ask.
- **If user picks 8j:** start by checking MailerLite domain verification status in the UI. Reconcile the doc contradiction (update AGENT-CONTEXT line 53 + the 8d/8g pending items to match reality). THEN design against `packages/ds/` cascade. Decide on the home card's "save 10% on your first order" claim (no discount mechanism exists — either build one, drop the claim, or replace with a benefit that's real).
- **If user picks 1e URL sweep:** grep for `kol-client-acyr-website.vercel.app` across all surfaces (canonical URLs, sitemap.xml, OG meta in seo-metadata, PayPal app return URLs in Vercel env + PayPal developer UI); replace with `another-creation.xyz`. Small focused PR.
- **If user picks Metadata Pass 2:** plan documented in `docs/client/kol-client-acyr/01-website/04-metadata/01-plan.md` §11. Schema additions to article + collection, Studio rebuild + redeploy, extend proxy lookup chain. ~half day.
- **If user picks something unrelated:** check the broader backlog — items 3 (multi-image + color variants — biggest commerce gap), 4 (bump FLAT_SHIPPING_EUR), 5 (14 polish sub-items), 9 (gallery image scaling / zoom), 13 (new — client overview doc).

## Working memory not yet in AGENT-CONTEXT

- **The `SpectrumControls.jsx` SVG-ID bug was real and was masking degraded rendering of the spectrum wheel** before the user even noticed the editor's 3-column-grid collapse. Phase 1's bulk rename hit the `id=` definitions but missed the `url(#…)` references that consume them. Lesson: when renaming an SVG fragment ID, you have to touch BOTH the definition AND every `url(#…)` reference; sed should match `id="kol-…"` AND `url(#kol-…)` AND `clipPath="url(#kol-…)"`. Watch for this pattern if any other SVG IDs need renaming later.
- **Acyr.jsx is the styleguide's internal "source-of-truth" page** at `brand.another-creation.xyz/acyr` (behind Cloudflare Access OTP). It's a developer/agency-internal reference, not user-facing. The Phase 4 strip of `COLLECTIONS`/`ARTICLES`/`AUTHORS` makes the page slightly less informative (snapshot prose lost a few counts, media inventory has placeholders) but doesn't break anything. If anyone wants to restore live counts: wire a Sanity client into the styleguide (`@sanity/client` + project ID env vars + a minimal GROQ helper) and rehydrate the counts via `useEffect` or build-time fetch.
- **`migrate-to-sanity.mjs` deserves either deletion or proper retirement.** Phase 4 chose retirement (kept-with-header). Argument for deletion: it's broken anyway, anyone needing the pattern can read it in git history. Argument for keeping: 218 lines of well-commented Portable Text conversion + SHA-1 asset dedup logic that's annoying to rewrite. Recommendation: keep for now, delete when the file gets in someone's way.
- **`@website-data` alias is technically retire-able with a one-line change** if you move `shop-data.js` from `apps/website/src/data/` to `packages/brand-data/` and update both the website + the styleguide imports. But shop-data is commerce data (POD products + handmade entries), not brand identity — semantically wrong for `brand-data`. Cleaner alternative: extract `packages/website-data/` (or `packages/commerce-data/`) just to host this one file. Overkill until there's a second consumer. Defer.
- **The "topology-note headers" on setup-overview + setup-playbook are a compromise.** Full rewrite would teach the workspace shape cleanly but is a half-day doc-rewrite that doesn't add functional value. Headers make the docs self-aware about being slightly behind. If anyone replicates the stack and runs into confusion, that's the trigger to do the full rewrite.
- **The `kol-client-acyr-*` Vercel project names are intentional and stay** — they're internal-only handles, never user-facing, and renaming them would mean creating new Vercel projects + reconfiguring DNS + losing deploy history. Phase 4 deliberately did not touch them. They're the only remaining `kol-` brand reference in active-runtime infrastructure.
- **Re-org timing for the record:** Phase 0 (research) ✅ 2026-05-17 morning. Phase 1 ✅ 2026-05-17 evening (`3a8eaa0`). Phases 2, 3, 4 all shipped 2026-05-18 — three branches, three merges, ~one calendar day of focused work to close the apps+packages migration. Faster than the 2.5–4 day estimate in `docs/client/kol-client-acyr/01-website/05-restructure/phases.md` because Phase 1's prior groundwork made the splits clean.
- **User constraints held the whole session:** no Co-Authored-By trailers, no direct git commits by me, no PRs by me. Permission system updated mid-session to narrow the git deny to destructive verbs only (`commit`, `push`, `reset`, `rebase`, `merge`, `checkout --`, `branch -D`, `remote`, `clean -f`, `clean -fd`, `stash drop`) and allow safe verbs (`status`, `diff`, `log`, `show`, `ls-files`, `mv`, `rm`, `add`, `restore --staged`). Keep this pattern.
