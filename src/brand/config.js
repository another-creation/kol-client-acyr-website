/**
 * Brand identity config — the single source of truth for client-brand naming.
 *
 * All user-facing references to the client brand name (page titles, aria
 * labels, portal labels, carousel captions, palette ids/labels) and asset-
 * path fragments (`/brand/<slug>/…`) resolve through this file.
 */

export const BRAND = {
  name:     'Another Creation',
  nameSlug: 'another-creation',
}
